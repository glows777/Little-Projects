获取一些信息
显示错误信息 定义formcontrol为input的父元素 然后增加类名 在通过small标签增加错误提示
显示正确 赋值给formcotrol的class就好
检查是否填完 先设置一个布尔 为flase 然后分别遍历这个数组 数组的数据是每个input的内容 判断 如果input的value的trim（）为空 知行错误的函数 更改判断的变量为true 否则 则显示正确的函数 最后返回这个判断 方便后面判断
检查两次密码是否正确 两个参数 两次密码的数值 判断 触发错误的函数 
检查长度是否合法 三个参数 一个input 还有min max 判断input的value的length是否符合范围
获取当前文件的名字 这个歌函数直接返回input的id的首个字母转化为大写 同时拼接后面的字符串 用slice（1) 首个的用charAt（0）
添加submit的时间监听器 函数的参数为事件e 然后记得添加e。preventDefault（） 然后判断是否完整 判断用户名长度 密码长度 密码是否相同